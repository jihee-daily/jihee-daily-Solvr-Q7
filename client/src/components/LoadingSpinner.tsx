import React from 'react';

const LoadingSpinner: React.FC = () => (
  <div className="w-16 h-16 border-4 border-sky-500 border-dashed rounded-full animate-spin border-t-transparent"></div>
);

export default LoadingSpinner; 